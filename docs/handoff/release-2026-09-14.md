# Release Spark — 2026-09-14

## Phạm vi

D-107–D-116 đang có trong working tree được giữ nguyên và đưa vào release cùng D-117–D-118: detail 880px, Nội dung 4.000 ký tự với bold/italic/underline và ⌘/Ctrl+B/I/U, nút sửa cùng hàng nhãn không cuộn theo nội dung, mặc định start chỉ dành cho task mới trong Hôm nay.

## Bằng chứng trước phát hành

- `npm run lint`: pass.
- `npm run typecheck`: pass.
- `npm test`: 15 files, 159 tests pass.
- `npm run build`: Next.js 16.3.1 pass.
- `SPARK_TEST_URL=http://localhost:3016 SPARK_TEST_OFFLINE=1 npm run test:browser`: **5/5 pass**, gồm cold-start offline mở editor lần đầu và lưu/reload giữ định dạng.
- Browser Chromium desktop 1280×800 và mobile 390×844: phím tắt, undo/redo, lưu/reload/cancel, 4.000/4.001 ký tự, quick-add defaults, detail 880px, nút sửa cố định khi cuộn, toolbar mobile 44px và editor 160px được kiểm tra.
- Supabase migration `20260914084232_item_basic_content_formatting` đã áp dụng; cột JSONB có mặt, RLS còn bật, hai constraints mới VALIDATED.
- SQL test trong bảng TEMP theo constraints production: nhận 4.000 emoji có định dạng, từ chối 4.001 ký tự; rollback toàn bộ fixture.
- Không gửi OTP, không ghi fixture vào dữ liệu tài khoản. Các browser test dùng profile mới và demo riêng.

## Giới hạn

Kiểm thử mobile là viewport Chromium; chưa kiểm tra lại iPhone thật/IME Safari và cloud sync giữa hai thiết bị đăng nhập trong phiên này. Cloud mapping, equality và durable queue được regression test; RLS isolation tài khoản thật thuộc checklist còn mở.

## Production và Git

- Deployment: `dpl_EHwJzsRo5RW5yW61xmcuWDrSMB7d`, READY, production.
- URL build: https://spark-ceatc2egr-thuanngo.vercel.app
- Domain: https://spark.thuanngo.com — HTTP 200 ngày 2026-09-14.
- Cả **5 browser tests pass trên domain production**, gồm cold-start offline; dữ liệu test nằm trong demo của browser profile riêng.
- `vercel logs <deployment> --level error --since 1h --scope thuanngo`: không có log lỗi trả về.
- Lệnh deploy: `npx --yes vercel@latest deploy --prod --yes --scope thuanngo`. CLI 59.16.0 cần scope rõ trong phiên này; bỏ scope đã trả Not authorized dù whoami đúng.
- Bản application deploy trùng nội dung mã nguồn của release commit; sau deploy chỉ cập nhật tài liệu kết quả kiểm tra.
- Commit/push bao gồm mọi thay đổi Spark đang chờ từ các phiên trước và thay đổi editor của phiên này. Artifact ngoài Spark trong `output/`, `outputs/` giữ nguyên, không đưa lên Git/Vercel.
